{
  "name": "chronicle-autofill",
  "version": "0.1.0",
  "private": true,
  "description": "Fill Greenhouse/Lever/Ashby applications from your Chronicle profile. Fill-only — never submits.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  }
}
